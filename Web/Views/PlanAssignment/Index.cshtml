@model IEnumerable<Infrastructure.Models.PlanAssignment>


@{
    ViewBag.Title = "State Account";
}

<h2>State Account</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            Residence Number
        </th>
        <th>
            User
        </th>
        @*<th>
            Quantity of details
        </th>*@
        <th>Actions</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Residence.IDResidence)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Residence.User.FirstName) @Html.DisplayFor(modelItem => item.Residence.User.LastName)
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.AssignmentDetail.Count)
        </td>*@
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id = item.IDAssignment }) |*@
            @Html.ActionLink("Details", "Details", new { id = item.IDAssignment }, new { @class = "btn btn-primary" })
            @*@Html.ActionLink("Delete", "Delete", new { id = item.IDAssignment })*@
        </td>
    </tr>
}

</table>
