@model IEnumerable<Infrastructure.Models.Reservation>

@{
    ViewBag.Title = "User Reservations";
}

<h2 class="my-3">User Reservations</h2>

@Html.ActionLink("Make a new reservation", "Create", null, new { @class = "btn btn-primary mt-2" })

<div class="table-responsive">
    <table class="table table-striped trCustom shadow-lg animate__animated animate__fadeIn " id="myTable">
        <thead>
            <tr class="table-dark">
                <th>
                    AREA
                </th>
                <th>
                    DATE
                </th>
                <th>
                    TIME
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Approved)
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Area.Name)
                    </td>
                    <td>
                        @*@Html.DisplayFor(modelItem => item.Start)*@
                        @item.Start.Date.ToShortDateString()
                    </td>
                    <td>
                        @*@Html.DisplayFor(modelItem => item.Finish)*@
                        @item.Start.ToShortTimeString() - @item.Finish.ToShortTimeString()
                    </td>
                    <td>
                        @if (item.Approved == true)
                        {
                            <p style="color:green;">Approved</p>
                        }
                        else if (item.Approved == false)
                        {
                            <p style="color:red;">Denied</p>
                        }
                        else if (item.Approved == null)
                        {
                            <p>Pending</p>
                        }
                    </td>
                </tr>
            }
        </tbody>



    </table>
</div>

